# Local-only reference material — client source files (original photos, PDFs,
# schedules) and the original Claude Design export. Never shipped, never committed.
_source/

# OS / editor cruft
.DS_Store
*.log
